import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: 'hsl(267, 91%, 56%)',
    },
    secondary: {
      main: 'hsl(45, 15%, 89%)',
    },
  },
  typography: {
    fontSize: 14,
  },
});
